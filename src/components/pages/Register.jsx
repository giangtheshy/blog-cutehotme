import React from "react";
import RegisterForm from "../Form/Register/RegisterForm";

const Register = () => {
  return (
    <section>
      <RegisterForm />
    </section>
  );
};

export default Register;
